plugin.tx_pxadealers {
	view {
		templateRootPath = {$plugin.tx_pxadealers.view.templateRootPath}
		partialRootPath = {$plugin.tx_pxadealers.view.partialRootPath}
		layoutRootPath = {$plugin.tx_pxadealers.view.layoutRootPath}
	}
	persistence {
		storagePid = {$plugin.tx_pxadealers.persistence.storagePid}
	}
	features {
		# uncomment the following line to enable the new Property Mapper.
		# rewrittenPropertyMapper = 1
	}
	settings {
		findClosestAjax {
			additionalListWrapperClass = row-fluid
			elementAdditionalClass = span3
			newRow = 4
			resultLimit = 12
			map {
				stylesJSON = {$plugin.tx_pxadealers.settings.map.stylesJSON}
				name = {$plugin.tx_pxadealers.settings.map.name}
				markerImage = {$plugin.tx_pxadealers.settings.map.markerImage}
			}
		}
		
		#countries.109.row = Rest of the world
		#countries.0.sweden = Sverige

		# Map settings
		map {
			stylesJSON = {$plugin.tx_pxadealers.settings.map.stylesJSON}
			name = {$plugin.tx_pxadealers.settings.map.name}
			markerImage = {$plugin.tx_pxadealers.settings.map.markerImage}
		}

		countryNameIso3Mapping {
			canada = can
			usa = usa
		}

		# Countries which would display separately in countries filter (list of statiic info tables countries uid's)
		mainCountries = 

		# Which language correspondes to which country (language uid = static info table country uid)
		# Ex.: 0 = 186 (default lang = sweden)
		languageCountryMapping {
		}

		# Change country name (displayed in the country filter)
		# Ex: 74 = UK (United Kingdom = UK)
		nameCountryMapping {
		}

		noImagePath = EXT:pxa_dealers/Resources/Public/Images/noimage.png

		#Show "All countries" option in the country filter select box"
		showAllCountriesOption = {$plugin.tx_pxadealers.settings.showAllCountriesOption}
		includeGoogleMaps = {$plugin.tx_pxadealers.settings.includeGoogleMaps}
        includeLocalScripts = {$plugin.tx_pxadealers.settings.includeLocalScripts}
        includeIsotope = {$plugin.tx_pxadealers.settings.includeIsotope}

	}
}

page.includeCSS.pxa_dealers = EXT:pxa_dealers/Resources/Public/Css/pxa_dealers.css

findClosestAjax = PAGE
findClosestAjax.typeNum = 7505726
findClosestAjax.config.disableAllHeaderCode = 1
findClosestAjax.config.no_cache = 1
findClosestAjax.config.additionalHeaders = Content-type: application/json
findClosestAjax.config.disablePreviewNotification = 1
  
findClosestAjax.10 = USER
findClosestAjax.10 {
	userFunc = TYPO3\CMS\Extbase\Core\Bootstrap->run
	extensionName = PxaDealers
	pluginName = Pxadealerssearchresults
	vendorName = PXA
	switchableControllerActions {
		Dealers {
             1 = findClosestAjax
		}
	}
}